<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: 'Roboto', sans-serif;
      margin: 0;
      padding: 0;
      background-color: #f0f8ff;
      color: #333;
    }
    header {
      background-color: #4682b4;
      color: white;
      padding: 15px;
      text-align: center;
    }
    nav {
      display: flex;
      justify-content: center;
      background-color: #2b4d66;
      padding: 10px;
    }
    nav a, nav form button {
      color: white;
      text-decoration: none;
      margin: 0 10px;
      padding: 5px 10px;
      border-radius: 5px;
    }
    nav a:hover, nav form button:hover {
      background-color: #4682b4;
    }
    .container {
      padding: 20px;
    }
    ul {
      list-style-type: none;
      padding: 0;
    }
    ul li {
      background: #e9f0fa;
      margin: 10px 0;
      padding: 15px;
      border-radius: 5px;
      border: 1px solid #ccc;
    }
  </style>
</head>
<body>
  <header>
    <h1>Search Results</h1>
  </header>
  
  <nav>
    <a href="/">Home</a>
    <a href="/about">About</a>
    <a href="/quizzes">Quizzes</a>
    <a href="/add-quiz">Add Quiz</a>
    <form action="/search" method="GET" style="display: inline;">
      <input 
        type="text" 
        name="q" 
        placeholder="Search..." 
        style="padding: 5px; border-radius: 5px; border: 1px solid #ccc;"
        value="<%= query %>"
      >
      <button 
        type="submit" 
        style="background-color: #4682b4; color: white; border: none; padding: 5px 10px; border-radius: 5px; cursor: pointer;"
      >
        Search
      </button>
    </form>
  </nav>
  
  <div class="container">
    <h2>Results for "<%= query %>":</h2>
    <ul>
      <% if (results.length === 0) { %>
        <li>No results found.</li>
      <% } else { %>
        <% results.forEach(result => { %>
          <li><strong><%= result.title %></strong> - <%= result.category %></li>
        <% }); %>
      <% } %>
    </ul>
  </div>
</body>
</html>
